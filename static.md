# 文思有品 静态页面需求

## 1. 项目概述

参考"文思有品"平台的视觉风格和功能布局，实现以下静态页面（纯前端，无后端依赖）：

- 首页
- 饰品市场
- 我的库存
- 饰品详情
- 购买饰品

---

## 2. 公共部分

### 顶部导航栏

- 左侧：Logo（文思有品 + 标语 "租饰品 买饰品 上文思有品"）
- 中间：导航 Tab（首页、饰品市场、我的库存），带选中高亮下划线
- 右侧：消息通知图标、邮件图标、用户头像

### 主题色

- 主色：蓝色（#3b82f6 或类似）
- Logo：金色（#f0c040 或类似）
- 背景：浅灰色 / 白色

---

## 3. 首页

### Banner 区域

- 大标题："文思有品APP"
- 副标题："周周0元免费租 百万饰品官方补贴"
- 右侧：CS2 角色图片（Joker 面具 + 礼盒）
- 蓝色渐变背景

### 周周免费租 横滑卡片

- 左侧标题："周周免费租"
- 一排横向滚动的饰品卡片（匕首、手套等）
- 每张卡片显示：饰品图标 + "¥0/天" 标签
- 右侧："查看更多 >" 按钮

### 买饰品 区域

- 标题："买饰品"
- Tab："最新上架"（带蓝色下划线）
- 右侧："进入市场 >" 按钮
- 下方：饰品卡片网格（5列），每张卡片包含：
  - 品质标签（如"久经沙场"、"大师"、"崭新出厂"、"闪耀"、"全息"）
  - 稀有度星星（紫色星标）
  - 饰品图片
  - 饰品名称

---

## 4. 饰品市场

### 分类筛选区

- 顶部：武器分类图标横排（匕首、手枪、步枪、微型冲锋枪、霰弹枪、机枪、手套、其他、印花、装备、挂件）
- 中间：筛选条件（品质、类别、外观、颜色、收藏品、印花搜枪）
- 右侧：搜索框 + "搜索"按钮

### 交易方式 Tab

- "出售"（选中，蓝色下划线）
- "租赁"

### 价格区间 & 排序

- 最低价输入框
- 最高价输入框
- 排序下拉框

### 饰品列表

- 网格布局（5列）
- 每张卡片包含：
  - 品质/磨损标签（金色、略有磨损、崭新出厂等）
  - 稀有度星标
  - 饰品图片
  - 饰品全名
  - 价格（红色字体，如 ¥174.5）
  - 在售数量（灰色小字，如 "132在售"）

---

## 5. 我的库存

### 标题栏

- 左侧："Steam库存"
- 右侧："件数: --  估值: --"

### 筛选条件

- 类型、品质、类别、外观、状态（下拉选择）
- 搜索框 + "搜索"按钮

### 操作栏

- 表格表头：时间（可排序）、价格（可排序）、磨损（可排序）
- 已选：0/0
- 全选复选框
- 按钮：合并、刷新库存、出售上架、租赁上架

### 内容区

- 当前为空表状态，展示占位

---

## 6. 饰品详情

### 页面结构

- 左侧：饰品大图（3D 展示）
- 右侧：
  - 饰品名称 + 品质标签 + 稀有度
  - 价格（大号红色字体）
  - 磨损值
  - 卖家信息
  - "立即购买" / "加入收藏" 按钮

### 下方信息区

- 饰品属性详情（磨损值、图案模板、特殊属性等）
- 价格历史曲线（可选）

---

## 7. 购买饰品

### 弹窗/页面

- 确认购买信息：
  - 饰品名称、图片
  - 价格
  - 交易方式选择（Steam 报价 / 平台担保）
- 确认按钮

### 流程

1. 点击购买 → 弹出确认弹窗
2. 选择支付方式（可选）
3. 确认后创建订单
4. 跳转订单状态页

---

## 8. 技术实现

- 纯静态页面，使用 mock 数据
- 保留现有 React + Vite + Ant Design + TypeScript 技术栈
- 路由新增：`/`（首页）、`/market`（饰品市场）、`/inventory`（我的库存）、`/item/:id`（饰品详情）、`/purchase`（购买）
- 原有 MarketPage/OrdersPage/LoginPage 保留或替换
- 数据全部硬编码在 mock 文件中
- 保留 Vite proxy 配置，方便后续对接真实 API

---

## 9. 数据 Mock

需要 mock 的数据结构：

### Item

```typescript
interface Item {
  id: string
  name: string
  iconUrl: string
  quality: string       // 久经沙场/崭新出厂/略有磨损/闪耀等
  rarity: string        // 普通/军规/受限/保密/隐秘/非凡
  price: number
  salesCount: number    // 在售数量
  wear?: number         // 磨损值 0-1
  sticker?: string[]    // 印花
}
```

### Category

```typescript
interface Category {
  key: string
  name: string
  icon: string
}
```
